const { endpointsSettings, jwtSettings, serverSettings, keyPubPriFront, keyPubPriBack } = require('./config')

module.exports = Object.assign({}, { endpointsSettings, jwtSettings, serverSettings, keyPubPriFront, keyPubPriBack })

